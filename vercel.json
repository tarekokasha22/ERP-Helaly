{
    "buildCommand": "cd client && npm ci --legacy-peer-deps && npm run build",
    "outputDirectory": "client/build",
    "installCommand": "npm cache clean --force && cd client && npm ci --legacy-peer-deps",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}