# دليل نشر نظام الهلالي لإدارة المشاريع على UltraHost
# Al-Helaly Construction ERP Deployment Guide

## 📋 المحتويات | Table of Contents

1. [التحضير قبل النشر](#1-التحضير-قبل-النشر)
2. [إعداد الملفات](#2-إعداد-الملفات)
3. [الرفع على UltraHost](#3-الرفع-على-ultrahost)
4. [إعدادات UltraHost](#4-إعدادات-ultrahost)
5. [اختبار النشر](#5-اختبار-النشر)
6. [مراقبة والصيانة](#6-مراقبة-والصيانة)
7. [حل المشاكل](#7-حل-المشاكل)

---

## 1. التحضير قبل النشر | Pre-Deployment Preparation

### 1.1 تجهيز البيئة المحلية | Prepare Local Environment

```bash
# الانتقال لمجلد المشروع
cd helaly-erp/client

# التأكد من تثبيت جميع المكتبات
npm install

# بناء المشروع للإنتاج
npm run build
```

### 1.2 فحص ملفات البناء | Check Build Files

بعد البناء، يجب أن تتأكد من وجود المجلد التالي:
`helaly-erp/client/build/`

يجب أن يحتوي على:
- `index.html`
- `static/` (CSS و JS)
- `logo.png`
- `logo2.webp`
- `manifest.json`
- `robots.txt`

### 1.3 إزالة console.log (اختياري) | Remove console.log (Optional)

تم بالفعل إعداد الكود لإزالة console.log في الإنتاج، لكن يمكنك التأكد من ذلك في:
- `App.tsx`
- `Dashboard.tsx`
- جميع ملفات `.tsx` و `.ts`

---

## 2. إعداد الملفات | File Setup

### 2.1 نسخ ملفات النشر | Copy Deployment Files

انسخ جميع الملفات من مجلد `deployment/` إلى مجلد `build/`:

```bash
# نسخ .htaccess
cp deployment/.htaccess client/build/.htaccess

# نسخ robots.txt (إذا لم يكن موجوداً)
cp deployment/robots.txt client/build/robots.txt

# نسخ sitemap.xml
cp deployment/sitemap.xml client/build/sitemap.xml

# نسخ index.html المحسّن (اختياري - يمكنك استخدام الموجود)
cp deployment/index.html.optimized client/build/index.html
```

### 2.2 تحديث معلومات النطاق | Update Domain Information

**في ملف `sitemap.xml`:**
- استبدل `yourdomain.com` بنطاقك الفعلي

**في ملف `robots.txt`:**
- استبدل `https://yourdomain.com/sitemap.xml` بنطاقك

**في ملف `index.html`:**
- استبدل جميع `yourdomain.com` بنطاقك

**في ملف `.htaccess`:**
- راجع إعدادات www/non-www واختر المناسب

### 2.3 هيكل المجلدات | Folder Structure

```
build/
├── index.html
├── .htaccess
├── robots.txt
├── sitemap.xml
├── manifest.json
├── logo.png
├── logo2.webp
└── static/
    ├── css/
    │   └── main.*.css
    └── js/
        └── *.js
```

---

## 3. الرفع على UltraHost | Upload to UltraHost

### 3.1 الطريقة الأولى: cPanel File Manager

#### خطوات الرفع:

1. **الدخول إلى cPanel**
   - افتح: `https://yourdomain.com:2083` أو `https://cpanel.yourdomain.com`
   - سجّل دخولك

2. **فتح File Manager**
   - ابحث عن "File Manager" أو "مدير الملفات"
   - افتح المجلد `public_html/` أو `www/`

3. **حذف الملفات القديمة (إن وجدت)**
   - حدد جميع الملفات الموجودة
   - احذفها أو أنشئ مجلد نسخة احتياطية

4. **رفع الملفات**
   - انقر على "Upload" أو "رفع"
   - اختر جميع الملفات من مجلد `build/`
   - **مهم**: تأكد من رفع `.htaccess` (قد تحتاج لتفعيل إظهار الملفات المخفية)

5. **تفعيل إظهار الملفات المخفية**
   - في File Manager: Settings → Show Hidden Files

6. **نقل الملفات إلى public_html**
   - بعد الرفع، انقل جميع الملفات من مجلد `uploads/` إلى `public_html/`

### 3.2 الطريقة الثانية: FTP (FileZilla)

#### إعداد FileZilla:

1. **فتح FileZilla**
   - قم بتنزيل FileZilla من: https://filezilla-project.org/

2. **إعداد الاتصال**
   ```
   Host: ftp.yourdomain.com أو IP الخادم
   Username: اسم المستخدم من UltraHost
   Password: كلمة المرور
   Port: 21
   ```

3. **الاتصال**
   - انقر على "Quickconnect" أو "اتصال سريع"

4. **الانتقال للمجلد الصحيح**
   - في الجهة اليمنى (Remote site): انتقل إلى `public_html/`

5. **رفع الملفات**
   - في الجهة اليسرى (Local site): انتقل إلى مجلد `build/`
   - حدد جميع الملفات
   - اسحبها إلى `public_html/` أو انقر "Upload"

6. **تأكد من رفع .htaccess**
   - في FileZilla: View → Show Hidden Files

### 3.3 إعدادات مهمة للرفع | Important Upload Settings

- **نقل الملفات**: يجب نقل محتويات مجلد `build/` مباشرة إلى `public_html/`
- **ملف .htaccess**: يجب أن يكون موجوداً في الجذر
- **الصلاحيات**: تأكد من أن الصلاحيات صحيحة:
  - الملفات: `644`
  - المجلدات: `755`

---

## 4. إعدادات UltraHost | UltraHost Configuration

### 4.1 إعداد SSL Certificate

1. **الدخول إلى cPanel**
2. **البحث عن "SSL/TLS Status"**
3. **اختيار "Let's Encrypt"** (مجاني)
4. **اختيار النطاق**
5. **تثبيت الشهادة**

**ملاحظة**: بعد تثبيت SSL، انتظر بضع دقائق حتى يصبح نشطاً.

### 4.2 إعداد قاعدة البيانات (للمستقبل) | Database Setup (Future)

إذا احتجت قاعدة بيانات في المستقبل:

1. **إنشاء قاعدة بيانات**
   - cPanel → MySQL Databases
   - أنشئ قاعدة بيانات جديدة
   - أنشئ مستخدم جديد
   - اربط المستخدم بقاعدة البيانات
   - امنح جميع الصلاحيات

2. **معلومات الاتصال**
   ```
   Host: localhost
   Database: username_database
   Username: username_user
   Password: your_password
   ```

### 4.3 إعداد البريد الإلكتروني | Email Setup

1. **إنشاء حساب بريد**
   - cPanel → Email Accounts
   - أنشئ حساب بريد جديد (مثل: info@yourdomain.com)
   - قم بإعداد كلمة المرور

2. **الوصول للبريد**
   - Webmail: `https://yourdomain.com:2096`
   - أو استخدم بريد عميل مثل Outlook

---

## 5. اختبار النشر | Post-Deployment Testing

### 5.1 قائمة الاختبار | Testing Checklist

#### ✅ الصفحات الأساسية
- [ ] الصفحة الرئيسية تفتح بدون أخطاء
- [ ] صفحة تسجيل الدخول تعمل
- [ ] لوحة التحكم (Dashboard) تعمل
- [ ] صفحة المشاريع (Projects) تعمل
- [ ] صفحة المقاطع (Sections) تعمل
- [ ] صفحة المخزون (Inventory) تعمل
- [ ] صفحة التقارير (Reports) تعمل
- [ ] صفحة الموظفين (Employees) تعمل
- [ ] صفحة المدفوعات (Payments) تعمل

#### ✅ الوظائف | Functionality
- [ ] تسجيل الدخول يعمل
- [ ] التنقل بين الصفحات يعمل
- [ ] تغيير اللغة (عربي/إنجليزي) يعمل
- [ ] جميع الأزرار والروابط تعمل
- [ ] النماذج (Forms) تعمل
- [ ] الرسائل والتشعارات تظهر

#### ✅ الأداء | Performance
- [ ] سرعة تحميل الصفحة < 3 ثواني
- [ ] الصور تظهر بشكل صحيح
- [ ] الخطوط العربية تظهر بشكل صحيح
- [ ] الأيقونات والصور تظهر

#### ✅ الاستجابة | Responsiveness
- [ ] يعمل على الحاسوب (Desktop)
- [ ] يعمل على الأجهزة اللوحية (Tablet)
- [ ] يعمل على الهواتف الذكية (Mobile)
- [ ] القوائم والتنقل يعمل بشكل صحيح

#### ✅ الأمان | Security
- [ ] HTTPS يعمل (https://yourdomain.com)
- [ ] لا توجد أخطاء في Console
- [ ] Security Headers مفعلة (تحقق من Network tab)
- [ ] لا يمكن الوصول للملفات الحساسة

#### ✅ SEO
- [ ] Meta tags موجودة
- [ ] robots.txt يعمل
- [ ] sitemap.xml يعمل
- [ ] العنوان والوصف يظهران في نتائج البحث

---

## 6. مراقبة والصيانة | Monitoring & Maintenance

### 6.1 مراقبة الأخطاء | Error Monitoring

**الأخطاء في Console:**
1. افتح Developer Tools (F12)
2. تبويب Console
3. تحقق من وجود أخطاء

**الأخطاء في Network:**
1. Developer Tools → Network
2. تحقق من طلبات HTTP
3. تأكد من أن جميع الطلبات ناجحة (200, 304)

### 6.2 النسخ الاحتياطي | Backup Strategy

#### نسخ احتياطي أسبوعي:
1. **نسخ الملفات**
   - cPanel → File Manager
   - ضغط مجلد `public_html/`
   - تحميل النسخة

2. **نسخ قاعدة البيانات** (إن وجدت)
   - cPanel → phpMyAdmin
   - تصدير قاعدة البيانات

### 6.3 مراقبة الأداء | Performance Monitoring

**أدوات مجانية:**
- Google PageSpeed Insights: https://pagespeed.web.dev/
- GTmetrix: https://gtmetrix.com/
- Pingdom: https://tools.pingdom.com/

### 6.4 تحديثات منتظمة | Regular Updates

- **مرة شهرياً**: تحقق من تحديثات المكتبات
- **مرة أسبوعياً**: تحقق من الأخطاء في السجلات
- **يومياً**: تحقق من عمل الموقع الأساسي

---

## 7. حل المشاكل | Troubleshooting

### 7.1 الصفحة البيضاء | White Screen

**الحل:**
1. تحقق من ملف `.htaccess` - قد يكون فيه خطأ
2. تحقق من Console للأخطاء
3. تحقق من صلاحيات الملفات
4. جرب حذف `.htaccess` مؤقتاً لاختبار

### 7.2 أخطاء 404 | 404 Errors

**الحل:**
1. تأكد من أن ملف `.htaccess` موجود
2. تأكد من تفعيل `mod_rewrite` في UltraHost
3. تحقق من أن الملفات موجودة في `public_html/`

### 7.3 بطء التحميل | Slow Loading

**الحل:**
1. تحقق من ضغط GZIP (في `.htaccess`)
2. تحقق من Cache Headers
3. تحسين الصور (استخدام WebP)
4. تحقق من حجم ملفات JS/CSS

### 7.4 مشاكل SSL | SSL Issues

**الحل:**
1. انتظر 10-15 دقيقة بعد التثبيت
2. تحقق من تثبيت الشهادة بشكل صحيح
3. تأكد من إعادة التوجيه HTTPS في `.htaccess`

### 7.5 الخطوط العربية لا تظهر | Arabic Fonts Not Showing

**الحل:**
1. تحقق من اتصال الإنترنت (للحصول على Google Fonts)
2. تحقق من Content-Security-Policy في `.htaccess`
3. تأكد من تحميل الخطوط في `index.html`

### 7.6 مشاكل في التوجيه | Routing Issues

**الحل:**
1. تأكد من وجود قاعدة `RewriteRule` في `.htaccess`
2. تأكد من تفعيل `mod_rewrite`
3. تأكد من أن قاعدة Rewrite صحيحة

---

## 8. معلومات الاتصال والدعم | Contact & Support

### دعم UltraHost
- **الموقع**: https://www.ultrahost.com/
- **الدعم الفني**: من خلال cPanel أو البريد الإلكتروني
- **الدردشة المباشرة**: متاحة في لوحة التحكم

### موارد إضافية
- **React Router**: https://reactrouter.com/
- **React Deployment**: https://create-react-app.dev/docs/deployment/
- **Apache .htaccess**: https://httpd.apache.org/docs/current/howto/htaccess.html

---

## ✅ قائمة التحقق النهائية | Final Checklist

قبل إعلان الموقع جاهزاً:

- [ ] جميع الصفحات تعمل
- [ ] SSL مفعّل ويعمل
- [ ] السرعة مقبولة (< 3 ثواني)
- [ ] يعمل على جميع الأجهزة
- [ ] لا توجد أخطاء في Console
- [ ] SEO config صحيح
- [ ] النسخ الاحتياطي جاهز
- [ ] المراقبة مفعلة

---

**تم بنجاح! 🎉**

إذا واجهت أي مشاكل، راجع قسم "حل المشاكل" أعلاه أو اتصل بدعم UltraHost.

