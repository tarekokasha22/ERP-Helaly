# 🔧 إعداد المشروع بعد إعادة تعيين Windows

## المشكلة
بعد إعادة تعيين Windows، تم حذف مجلدات `node_modules` (وهذا طبيعي).

## الحل السريع

### الطريقة الأولى: استخدام سكربت التثبيت (الأسهل)
من **جذر المشروع**، نفّذ:
```bash
npm run install:all
```

### الطريقة الثانية: التثبيت اليدوي

#### 1. تثبيت حزم Client:
```bash
cd client
npm install
```

#### 2. تثبيت حزم Server:
```bash
cd server
npm install
```

#### 3. بناء Server:
```bash
cd server
npm run build
```

---

## تشغيل المشروع

### تشغيل السيرفر:
```bash
npm run start:server
```
أو:
```bash
cd server
npm run dev
```

### تشغيل الواجهة (في Terminal منفصل):
```bash
npm start
```
أو:
```bash
cd client
npm start
```

---

## ملاحظات مهمة

✅ **Node.js و npm مثبتان** (v24.13.0 و 11.6.2)

✅ **ملفات .env موجودة** في:
- `client/.env`
- `server/.env`

✅ **البيانات محفوظة** في:
- `server/data/` (جميع ملفات JSON موجودة)

⚠️ **لا تحتاج MongoDB** - المشروع يستخدم JSON storage

---

## إذا ظهرت أخطاء

### خطأ: "Cannot find module"
- تأكد من تثبيت الحزم: `npm install` في المجلد المناسب

### خطأ: "Port already in use"
- أغلق البرنامج الذي يستخدم المنفذ 3000 أو 5000

### خطأ: "TypeScript compilation failed"
- تأكد من تثبيت حزم Server: `cd server && npm install`

---

**بعد التثبيت، المشروع سيعمل بشكل طبيعي! 🚀**
