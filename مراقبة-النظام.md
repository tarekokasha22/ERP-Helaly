# 📊 دليل مراقبة نظام الهلالي للمقاولات

## 🌐 مراقبة الخادم (Server)

### 1. تشغيل الخادم
```bash
cd helaly-erp/server
npm start
```

**الخادم يعمل على:** `http://localhost:5000`

### 2. فحص حالة الخادم
```bash
# في PowerShell
Invoke-WebRequest -Uri http://localhost:5000 -UseBasicParsing

# أو في المتصفح
http://localhost:5000
```

**الاستجابة المتوقعة:**
```json
{
  "message": "🏗️ Al-Helaly Construction ERP API",
  "version": "1.0.0",
  "documentation": "/api/docs",
  "health": "/health"
}
```

---

## 🗂️ مراقبة قاعدة البيانات

### مكان ملفات البيانات:
```
helaly-erp/server/data/
├── projects.json    # المشاريع
├── sections.json    # الأقسام  
├── spendings.json   # المصروفات
└── users.json       # المستخدمين
```

### إحصائيات البيانات الحالية:

#### 📋 المشاريع:
- **مصر:** 5 مشاريع طرق (148 مليون ج.م)
- **ليبيا:** 4 مشاريع طرق (95 مليون دينار)
- **الإجمالي:** 9 مشاريع طرق

#### 🏗️ الأقسام:
- **مصر:** 11 قسم متخصص
- **ليبيا:** 9 أقسام متخصصة  
- **الإجمالي:** 20 قسم

#### 💰 المصروفات:
- **مصر:** 11 مصروف (84.5 مليون ج.م)
- **ليبيا:** 9 مصروفات (65.2 مليون دينار)
- **الإجمالي:** 20 مصروف

---

## 🔍 طرق مراقبة API

### 1. اختبار المصادقة
```powershell
# تسجيل الدخول
$body = @{
    username = 'admin'
    password = 'password'
    country = 'egypt'
} | ConvertTo-Json

$response = Invoke-WebRequest -Uri http://localhost:5000/api/auth/login -Method POST -Body $body -ContentType 'application/json' -UseBasicParsing

# استخراج التوكين
$result = $response.Content | ConvertFrom-Json
$token = $result.token
Write-Host "Token: $token"
```

### 2. فحص المشاريع
```powershell
# فحص جميع المشاريع
$headers = @{ Authorization = "Bearer $token" }
$projects = Invoke-WebRequest -Uri http://localhost:5000/api/projects -Headers $headers -UseBasicParsing

# عرض النتيجة
$projectsData = ($projects.Content | ConvertFrom-Json).data
Write-Host "عدد المشاريع: $($projectsData.Count)"
```

### 3. فحص الأقسام
```powershell
# فحص جميع الأقسام
$sections = Invoke-WebRequest -Uri http://localhost:5000/api/sections -Headers $headers -UseBasicParsing
$sectionsData = ($sections.Content | ConvertFrom-Json).data
Write-Host "عدد الأقسام: $($sectionsData.Count)"
```

### 4. فحص المصروفات
```powershell
# فحص جميع المصروفات
$spendings = Invoke-WebRequest -Uri http://localhost:5000/api/spendings -Headers $headers -UseBasicParsing
$spendingsData = ($spendings.Content | ConvertFrom-Json).data
Write-Host "عدد المصروفات: $($spendingsData.Count)"
```

### 5. فحص لوحة التحكم
```powershell
# فحص إحصائيات لوحة التحكم
$dashboard = Invoke-WebRequest -Uri http://localhost:5000/api/dashboard -Headers $headers -UseBasicParsing
$dashboardData = $dashboard.Content | ConvertFrom-Json
Write-Host "إجمالي المشاريع: $($dashboardData.projectStats.total)"
Write-Host "المشاريع النشطة: $($dashboardData.projectStats.active)"
Write-Host "إجمالي الميزانية: $($dashboardData.financialStats.totalBudget)"
```

---

## 🧪 أدوات الاختبار

### 1. ملف الاختبار الشامل
**المكان:** `helaly-erp/test-system.html`

**كيفية الاستخدام:**
1. افتح الملف في المتصفح
2. انقر على "اختبار شامل للنظام"
3. راقب النتائج والإحصائيات

### 2. اختبار تفاصيل مشروع محدد
```powershell
# اختبار تفاصيل مشروع مصري
$projectDetails = Invoke-WebRequest -Uri "http://localhost:5000/api/projects/EGY001" -Headers $headers -UseBasicParsing
$projectData = ($projectDetails.Content | ConvertFrom-Json).data

Write-Host "اسم المشروع: $($projectData.name)"
Write-Host "عدد الأقسام: $($projectData.sectionsCount)"
Write-Host "إجمالي المنصرف: $($projectData.totalSpent)"
Write-Host "المتبقي من الميزانية: $($projectData.remainingBudget)"
```

### 3. اختبار تفاصيل قسم محدد
```powershell
# اختبار تفاصيل قسم معين
$sectionDetails = Invoke-WebRequest -Uri "http://localhost:5000/api/sections/SEC001" -Headers $headers -UseBasicParsing
$sectionData = ($sectionDetails.Content | ConvertFrom-Json).data

Write-Host "اسم القسم: $($sectionData.name)"
Write-Host "اسم المشروع: $($sectionData.projectName)"
Write-Host "إجمالي المنصرف: $($sectionData.totalSpent)"
Write-Host "المتبقي: $($sectionData.remainingBudget)"
```

---

## 📈 مراقبة الأداء

### 1. سرعة الاستجابة
```powershell
# قياس زمن الاستجابة
$stopwatch = [System.Diagnostics.Stopwatch]::StartNew()
$response = Invoke-WebRequest -Uri http://localhost:5000/api/projects -Headers $headers -UseBasicParsing
$stopwatch.Stop()
Write-Host "زمن الاستجابة: $($stopwatch.ElapsedMilliseconds) مللي ثانية"
```

### 2. مراقبة استخدام الذاكرة
```powershell
# فحص عمليات Node.js
Get-Process -Name node | Select-Object Id, ProcessName, WorkingSet, CPU
```

### 3. مراقبة ملفات السجل
**المكان:** `helaly-erp/server/server_log.txt`

```powershell
# عرض آخر 20 سطر من السجل
Get-Content "helaly-erp/server/server_log.txt" -Tail 20
```

---

## 🔄 مراقبة التحديثات في الوقت الفعلي

### 1. مراقبة إضافة مشروع جديد
1. افتح تطبيق الويب: `http://localhost:3000`
2. سجل دخولك بـ `admin` / `password` / `egypt`
3. اذهب إلى المشاريع واضغط "مشروع جديد"
4. أضف مشروع جديد
5. تحقق من التحديث الفوري في لوحة التحكم

### 2. مراقبة قاعدة البيانات
```powershell
# فحص حجم ملفات البيانات
Get-ChildItem "helaly-erp/server/data/*.json" | Select-Object Name, Length, LastWriteTime
```

### 3. مراقبة API في الوقت الفعلي
```powershell
# فحص دوري كل 30 ثانية
while ($true) {
    $dashboard = Invoke-WebRequest -Uri http://localhost:5000/api/dashboard -Headers $headers -UseBasicParsing
    $data = $dashboard.Content | ConvertFrom-Json
    Write-Host "$(Get-Date): مشاريع = $($data.projectStats.total), ميزانية = $($data.financialStats.totalBudget)"
    Start-Sleep 30
}
```

---

## 🚨 استكشاف الأخطاء

### 1. الخادم لا يعمل
```bash
# تحقق من المنفذ
netstat -an | findstr ":5000"

# إعادة تشغيل الخادم
cd helaly-erp/server
npm start
```

### 2. خطأ في قاعدة البيانات
```powershell
# فحص سلامة ملفات JSON
Get-Content "helaly-erp/server/data/projects.json" | ConvertFrom-Json
```

### 3. خطأ في المصادقة
```powershell
# مسح التوكينات وإعادة تسجيل الدخول
# في المتصفح: F12 > Application > Local Storage > Clear
```

### 4. مراقبة الأخطاء في السجل
```bash
# عرض الأخطاء فقط
Get-Content "helaly-erp/server/server_log.txt" | Select-String "error"
```

---

## 📊 لوحة مراقبة سريعة

### PowerShell Script للمراقبة السريعة:

```powershell
# ===== سكريبت مراقبة سريع =====

Write-Host "🔍 فحص نظام الهلالي للمقاولات..." -ForegroundColor Green

# 1. فحص الخادم
try {
    $serverCheck = Invoke-WebRequest -Uri http://localhost:5000 -UseBasicParsing -TimeoutSec 5
    Write-Host "✅ الخادم يعمل بنجاح" -ForegroundColor Green
} catch {
    Write-Host "❌ الخادم لا يعمل!" -ForegroundColor Red
    exit
}

# 2. تسجيل الدخول
$loginBody = @{
    username = 'admin'
    password = 'password'
    country = 'egypt'
} | ConvertTo-Json

try {
    $loginResponse = Invoke-WebRequest -Uri http://localhost:5000/api/auth/login -Method POST -Body $loginBody -ContentType 'application/json' -UseBasicParsing
    $token = ($loginResponse.Content | ConvertFrom-Json).token
    $headers = @{ Authorization = "Bearer $token" }
    Write-Host "✅ تم تسجيل الدخول بنجاح" -ForegroundColor Green
} catch {
    Write-Host "❌ فشل تسجيل الدخول!" -ForegroundColor Red
    exit
}

# 3. فحص البيانات
$projects = Invoke-WebRequest -Uri http://localhost:5000/api/projects -Headers $headers -UseBasicParsing
$sections = Invoke-WebRequest -Uri http://localhost:5000/api/sections -Headers $headers -UseBasicParsing
$spendings = Invoke-WebRequest -Uri http://localhost:5000/api/spendings -Headers $headers -UseBasicParsing
$dashboard = Invoke-WebRequest -Uri http://localhost:5000/api/dashboard -Headers $headers -UseBasicParsing

$projectsData = ($projects.Content | ConvertFrom-Json).data
$sectionsData = ($sections.Content | ConvertFrom-Json).data
$spendingsData = ($spendings.Content | ConvertFrom-Json).data
$dashboardData = $dashboard.Content | ConvertFrom-Json

# 4. عرض الإحصائيات
Write-Host "`n📊 إحصائيات النظام:" -ForegroundColor Cyan
Write-Host "المشاريع: $($projectsData.Count)" -ForegroundColor Yellow
Write-Host "الأقسام: $($sectionsData.Count)" -ForegroundColor Yellow
Write-Host "المصروفات: $($spendingsData.Count)" -ForegroundColor Yellow
Write-Host "إجمالي الميزانية: $($dashboardData.financialStats.totalBudget) ج.م" -ForegroundColor Yellow
Write-Host "إجمالي المنصرف: $($dashboardData.financialStats.totalSpending) ج.م" -ForegroundColor Yellow

Write-Host "`n🎉 النظام يعمل بشكل مثالي!" -ForegroundColor Green
```

**احفظ هذا السكريبت في ملف:** `monitor-system.ps1`

---

## 🎯 الخلاصة

**لمراقبة النظام بشكل يومي:**

1. **تشغيل:** `npm start` في مجلد `helaly-erp/server`
2. **فحص:** افتح `http://localhost:5000` للتأكد من عمل الخادم
3. **مراقبة:** استخدم ملف `test-system.html` للفحص الشامل
4. **البيانات:** تحقق من ملفات JSON في `helaly-erp/server/data/`
5. **الأخطاء:** راجع `server_log.txt` لأي مشاكل

**🔗 الروابط المهمة:**
- الخادم: http://localhost:5000
- التطبيق: http://localhost:3000
- اختبار شامل: `helaly-erp/test-system.html`
