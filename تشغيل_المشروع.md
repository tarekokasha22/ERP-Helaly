# ✅ المشروع جاهز للتشغيل!

## ⚠️ مهم جداً: ترتيب التشغيل

**يجب تشغيل السيرفر أولاً ثم الواجهة.**  
إذا ظهرت رسالة **"Network Error"** أو **"خطأ في الشبكة"** فمعناها أن السيرفر غير مشغّل أو لا يستجيب.

---

## 🚀 كيفية التشغيل (بالترتيب الصحيح):

### الخطوة 1 - Terminal الأول: تشغيل السيرفر أولاً
```bash
npm run start:server
```
انتظر حتى ترى: `🚀 Server running on port 5000`

### الخطوة 2 - Terminal الثاني: تشغيل الواجهة
```bash
npm start
```
انتظر حتى يفتح المتصفح على: http://localhost:3000

---

### من جذر المشروع (بديل):

#### Terminal 1 - السيرفر:
```bash
npm run start:server
```
أو للتطوير (مع إعادة التشغيل التلقائي):
```bash
npm run dev:server
```

#### Terminal 2 - الواجهة:
```bash
npm start
```

---

### الطريقة الثانية: من المجلدات مباشرة

#### Terminal 1 - السيرفر:
```bash
cd server
npm start
```
أو للتطوير:
```bash
cd server
npm run dev
```

#### Terminal 2 - الواجهة:
```bash
cd client
npm start
```

---

## 🌐 الروابط:

- **الواجهة:** http://localhost:3000
- **السيرفر:** http://localhost:5000
- **API Health Check:** http://localhost:5000/health

---

## 🔐 بيانات الدخول الافتراضية:

- **Username:** `admin`
- **Password:** `admin123`
- متاح لمصر وليبيا

---

## ⚠️ ملاحظات:

1. **لا تحتاج MongoDB** - المشروع يستخدم JSON storage. عند التشغيل قد ترى رسالة أن الموظفين والمدفوعات تُهيأ في JSON storage (هذا طبيعي).
2. **البيانات محفوظة** في `server/data/` - لن تفقد بياناتك
3. إذا ظهر خطأ "Port already in use"، أغلق البرنامج الذي يستخدم المنفذ

---

---

## 🔧 إذا ظهر "Network Error" أو "خطأ في الشبكة"

| السبب | الحل |
|--------|-----|
| السيرفر غير مشغّل | شغّل السيرفر أولاً: `npm run start:server` في terminal منفصل، ثم حدّث الصفحة في المتصفح. |
| السيرفر توقف | أعد تشغيل السيرفر من مجلد المشروع. |
| غيّرت ملف `.env` في الـ client | أوقف الواجهة (Ctrl+C) ثم شغّلها مرة أخرى: `npm start`. |
| المنفذ 5000 مستخدم | غيّر في `server/.env`: `PORT=5001` ثم في `client/.env`: `REACT_APP_API_URL=http://localhost:5001/api` وأعد تشغيل الاثنين. |

**المشروع جاهز! 🎉**
