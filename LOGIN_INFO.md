# معلومات تسجيل الدخول - نظام إدارة شركة الهلالي

## 🔑 بيانات تسجيل الدخول الافتراضية

### للمدير العام:
- **البريد الإلكتروني:** `admin@helaly.com`  
- **كلمة المرور:** `password`
- **الدولة:** `egypt` أو `libya`

## 🌐 روابط النظام

- **الخادم (API):** http://localhost:5000
- **العميل (واجهة المستخدم):** http://localhost:3000

## ✅ التحقق من عمل النظام

### 1. اختبار الخادم:
```bash
curl http://localhost:5000
```

### 2. اختبار تسجيل الدخول:
```powershell
$body = @{email='admin@helaly.com'; password='password'; country='egypt'} | ConvertTo-Json
Invoke-WebRequest -Uri http://localhost:5000/api/auth/login -Method POST -Body $body -ContentType 'application/json' -UseBasicParsing
```

## 🗂️ ملفات البيانات

البيانات محفوظة في: `server/data/`
- `users.json` - المستخدمين
- `projects.json` - المشاريع  
- `sections.json` - الأقسام
- `spendings.json` - المصروفات

## 🚀 تشغيل سريع

استخدم ملف `start-all.bat` لتشغيل النظام بالكامل:
```bash
./start-all.bat
```

## ⚠️ ملاحظات مهمة

1. **البيانات تبقى محفوظة** بعد إعادة التشغيل
2. **العميل لا يستخدم Mock API** بعد الآن
3. **جميع العمليات تتم عبر الخادم الحقيقي**
4. **فصل البيانات حسب الدولة** (مصر/ليبيا)